spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/foodtruck
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        default_schema: public

firebase:
  config-file: classpath:food-truck-f524e-firebase-adminsdk-2szft-579d5d1f9c.json
